# graphql-java-spring-boot-mysql
Sample spring boot app with graphql and mysql

to test the application make sure to have a mysql instance running on localhost
clone te repo and run the following command

```
gradle bootrun